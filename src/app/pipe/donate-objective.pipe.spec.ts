import { DonateObjectivePipe } from './donate-objective.pipe';

describe('DonateObjectivePipe', () => {
  it('create an instance', () => {
    const pipe = new DonateObjectivePipe();
    expect(pipe).toBeTruthy();
  });
});
